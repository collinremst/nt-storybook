import React from 'react';

export default function Button() {
  return (
    <div>
        <button>This is a button that functions as a radio button</button>
        <p>When multiple radio buttons are being used, only one can be selected at the time </p>
    </div>
  );
}