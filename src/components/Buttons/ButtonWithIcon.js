import React from 'react';

export default function Button() {
  return (
    <div>
        <button>This is a button that contains an icon</button>
        <p>This components extends the Button Base components and inherets its functionality.</p>
    </div>
  );
}