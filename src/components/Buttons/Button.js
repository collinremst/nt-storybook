import React from 'react';

export default function Button() {
  return (
    <div>
        <button>This is a button</button>
        <p>The base of the button component contains all the main principles for every button, like color, text, size, shape and other design tokens</p>
    </div>
  );
}